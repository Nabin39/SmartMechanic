# Evidence: APK / build outputs

Place exported APK/AAB files or EAS build links here after running:

```bash
eas build -p android --profile preview
```

Do **not** commit signing keys or keystore passwords.
